class Game {
  run() {
    // until all three disks are on one tower in ascending order
    // begin with three disks on one stick in ascending order of size
    // get move from player
    // only move disk if moving onto bigger disk or is empty
    // make move with disks
  }
}
