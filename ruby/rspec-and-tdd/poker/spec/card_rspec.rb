require 'rspec'

describe Card do
  describe "#initialize" do
    it "should initialize a card" do
      expect 
    end
  end

  describe "#to_s" do 
    it "returns out a readable version of card" do
      expect(card.to_s).to eq()
    end
  end

  describe "#<=>" do
    it "should compare one card to another card" do
      expect
    end

    it "should return -1 if left card is greater value" do
      expect (card.<==>(5, 0)).to eq(-1)
    end

    it "should return 0 if left card and right card are same value" do
      expect
    end

    it "should return 1 if right card is greater value" do 
  end
end